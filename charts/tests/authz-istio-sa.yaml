waas-controller:
  extraAnnotations: 
    proxy.istio.io/config: '{ "holdApplicationUntilProxyStarts": true }'

waas-enforcer:
  extraAnnotations: 
    proxy.istio.io/config: '{ "holdApplicationUntilProxyStarts": true }'